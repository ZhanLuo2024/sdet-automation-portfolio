[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    api: marks tests that verify API behavior.
    web: marks tests that verify Web UI behavior.
    smoke: marks the smallest set of checks for quick confidence.
    regression: marks broader checks that cover more behavior.
